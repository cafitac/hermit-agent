/**
 * Stub for Claude Code's src/utils/fullscreen.ts
 * Claude Code toggles mouse click reporting based on user preferences.
 * For the port we default to mouse clicks enabled.
 */

export function isMouseClicksDisabled(): boolean {
  return false;
}
