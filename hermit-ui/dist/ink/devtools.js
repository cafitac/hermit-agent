/**
 * Stub — devtools is optional; reconciler conditionally dynamic-imports it
 * when DEV env is set. Provide a no-op default to satisfy the type check.
 */
export default function connectDevtools(_reconciler) {
    // no-op default
}
