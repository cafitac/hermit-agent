# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
!hermit-ui/dist/
hermit-ui/dist/*
!hermit-ui/dist/app.js
!hermit-ui/dist/uiModel.js
!hermit-ui/dist/startupStatus.js

# Virtual environment
venv/
.venv/

# Claude Code original source (reference only, managed separately).
# Anchored to repo root so nested src/ directories (e.g. benchmark
# starters) are not accidentally ignored.
/src/
claw-code-main/

# OMC state
.omc/
.omx/

# Harness audit snapshots (per-machine, contain personal paths)
.harness/

# Ralph / ralplan workflow artifacts
progress.txt
prd.json

# OS
.DS_Store
*.swp
*.swo
*~
# macOS extended-attribute / resource-fork droppings
.!*!*

# IDE
.idea/
.vscode/
*.code-workspace

# Claude Code local-only settings and personal session data
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/learned-feedback/
.claude/skills/
.claude/hooks/auto_session_learning.py

# Sensitive information (never commit)
.env
.env.local
.env.*.local
*.pem
*.key
credentials*
secrets*

# Temporary files
*.tmp
*.log
hermit_scratchpad/

# Django test project
django/

# React test project
react/
node_modules/
hermit-channel/node_modules/

# HermitAgent runtime data
.hermit/

# Agent learner / codex runtime state (not project source)
.agent-learner/
.agents/
.codex/
.codex-channels/
plugins/

# Build artifacts
*.tgz

# DB files (runtime-generated)
*.db
*.db-shm
*.db-wal

# Temporary/local config files
task_state.md
claude_code_zai_env.sh

# AI-managed internal planning/design docs (not for public OSS)
.dev/*
!.dev/refactor/
!.dev/refactor/**

# Third-party type stubs (not project source)
*.pyi
mcp/
duckduckgo_search/

# Subrepos (managed separately)
hermes-agent/

# Local knowledge scratchpad
kb/

# Local git worktrees used for isolated agent tasks
.worktrees/
